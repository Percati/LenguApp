<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!--
        Ningun permiso de red. Ver proyecto/legal/PRIVACY.md: la ausencia del
        permiso INTERNET es la prueba verificable de que la app no puede
        conectarse a nada. No agregarlo, aunque algo parezca necesitar red.
    -->

    <!--
        androidx.glance:glance-appwidget depende de WorkManager, y el
        manifiesto de WorkManager declara estos cuatro permisos por si el
        consumidor usa su backend viejo basado en AlarmManager (WAKE_LOCK,
        RECEIVE_BOOT_COMPLETED) o restricciones de red (ACCESS_NETWORK_STATE)
        o trabajo en primer plano (FOREGROUND_SERVICE). Con minSdk 26,
        WorkManager usa siempre JobScheduler y nunca ese camino, y el widget
        no declara ninguna restriccion de red ni trabajo en primer plano: se
        sacan explicitamente porque no hacen falta, no porque se hayan
        pasado por alto.
    -->
    <uses-permission android:name="android.permission.WAKE_LOCK" tools:node="remove" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" tools:node="remove" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" tools:node="remove" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" tools:node="remove" />

    <application
        android:allowBackup="false"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@android:style/Theme.Material.Light.NoActionBar">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <receiver
            android:name=".widget.SemanaWidgetReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/semana_widget_info" />
        </receiver>
    </application>

</manifest>
